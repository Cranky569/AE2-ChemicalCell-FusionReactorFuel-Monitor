local bridge = peripheral.find("meBridge") or peripheral.find("me_bridge") or peripheral.find("bridge")

if not bridge then
  error("Bridge nicht gefunden!")
end

print("=== Verfügbare Methoden von bridge ===")
for _,name in ipairs(peripheral.getMethods(peripheral.getName(bridge))) do
  print("-", name)
end
