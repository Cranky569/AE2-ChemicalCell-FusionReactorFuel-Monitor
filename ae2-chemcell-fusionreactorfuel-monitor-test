-- Debug script to list all connected peripherals
print("=== Connected Peripherals ===\n")

local names = peripheral.getNames()

for _, name in ipairs(names) do
    local ptype = peripheral.getType(name) or "unknown"
    print(string.format("Name: %s | Type: %s", name, ptype))
end

print("\n=== End of List ===")
